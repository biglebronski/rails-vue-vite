#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
APP_DIR="$ROOT_DIR/test/dummy_html"

cd "$APP_DIR"
"$ROOT_DIR/bin/with-preferred-node" npm run dev -- --port 5174
