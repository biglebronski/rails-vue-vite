#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
APP_DIR="$ROOT_DIR/test/dummy_spa"

cd "$APP_DIR"
BUNDLE_USER_HOME=/tmp/bundle bundle install --local
npm install
